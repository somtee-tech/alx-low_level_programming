#include <stdio.h>

/**
 * main - a function that writes it name
 * @argc: argument of count
 * @argv: array of pointers to argument string
 * Return: 0
 */

int main(int argc, char *argv[])
{
	argc = argc;
	printf("%s\n", *argv);
	return (0);
}
