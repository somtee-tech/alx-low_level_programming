#include "main.h"

/**
 * int _strlen - Write a function that returns the length of a string.
 *
 */

int _strlen(char *s)
{


	    for (s = 0; Str[s] != '\0'; ++s);
	    printf("Length of Str is %d", s);
}
